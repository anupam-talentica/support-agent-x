# Minimal deps for RAG ingest script only (no a2a-sdk).
# ChromaDB's Settings model has unannotated fields; Pydantic 2.1+ enforces annotations.
# Pin to Pydantic 2.0.x only.
# Usage:
#   python3.12 -m venv .venv_rag_ingest
#   source .venv_rag_ingest/bin/activate
#   pip install -r scripts/requirements-rag-ingest.txt
#   python scripts/ingest_rag_data.py

pydantic>=2.0,<2.1
pydantic-settings>=2.0.0
chromadb>=0.5.0
openai>=1.0.0
